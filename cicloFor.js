// Usamos el ciclo for cuando sabemos cuantas iteraciones van a ser necesarias
// En los ciclos se puede usar var o let
var miArreglo = [];

for (let i = 0; i < 10; i += 2) {
  miArreglo.push(i);
}

console.log(miArreglo);
